# coding: utf-8

"""
    Wodby 2 Public API

    Public REST API for customer SDKs and code integrations. GraphQL remains internal for the dashboard. This contract is the versioned public surface. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from wodby.models.app_access_endpoint import AppAccessEndpoint
from wodby.models.app_access_resource import AppAccessResource
from wodby.models.app_access_setting import AppAccessSetting
from typing import Optional, Set
from typing_extensions import Self

class AppAccess(BaseModel):
    """
    AppAccess
    """ # noqa: E501
    id: StrictInt
    mode: StrictStr
    scope: StrictStr
    status: StrictStr
    settings: List[AppAccessSetting]
    public_routes_suppressed: StrictBool = Field(alias="publicRoutesSuppressed")
    effective_url: Optional[StrictStr] = Field(default=None, alias="effectiveUrl")
    last_error: Optional[StrictStr] = Field(default=None, alias="lastError")
    integration_id: StrictInt = Field(alias="integrationId")
    endpoints: List[AppAccessEndpoint]
    resources: List[AppAccessResource]
    created_at: datetime = Field(alias="createdAt")
    updated_at: datetime = Field(alias="updatedAt")
    __properties: ClassVar[List[str]] = ["id", "mode", "scope", "status", "settings", "publicRoutesSuppressed", "effectiveUrl", "lastError", "integrationId", "endpoints", "resources", "createdAt", "updatedAt"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['protected', 'private_network']):
            raise ValueError("must be one of enum values ('protected', 'private_network')")
        return value

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['entire_app', 'selected_endpoints']):
            raise ValueError("must be one of enum values ('entire_app', 'selected_endpoints')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AppAccess from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in settings (list)
        _items = []
        if self.settings:
            for _item_settings in self.settings:
                if _item_settings:
                    _items.append(_item_settings.to_dict())
            _dict['settings'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in endpoints (list)
        _items = []
        if self.endpoints:
            for _item_endpoints in self.endpoints:
                if _item_endpoints:
                    _items.append(_item_endpoints.to_dict())
            _dict['endpoints'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in resources (list)
        _items = []
        if self.resources:
            for _item_resources in self.resources:
                if _item_resources:
                    _items.append(_item_resources.to_dict())
            _dict['resources'] = _items
        # set to None if effective_url (nullable) is None
        # and model_fields_set contains the field
        if self.effective_url is None and "effective_url" in self.model_fields_set:
            _dict['effectiveUrl'] = None

        # set to None if last_error (nullable) is None
        # and model_fields_set contains the field
        if self.last_error is None and "last_error" in self.model_fields_set:
            _dict['lastError'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AppAccess from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "mode": obj.get("mode"),
            "scope": obj.get("scope"),
            "status": obj.get("status"),
            "settings": [AppAccessSetting.from_dict(_item) for _item in obj["settings"]] if obj.get("settings") is not None else None,
            "publicRoutesSuppressed": obj.get("publicRoutesSuppressed"),
            "effectiveUrl": obj.get("effectiveUrl"),
            "lastError": obj.get("lastError"),
            "integrationId": obj.get("integrationId"),
            "endpoints": [AppAccessEndpoint.from_dict(_item) for _item in obj["endpoints"]] if obj.get("endpoints") is not None else None,
            "resources": [AppAccessResource.from_dict(_item) for _item in obj["resources"]] if obj.get("resources") is not None else None,
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


